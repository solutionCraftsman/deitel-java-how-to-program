package one.five;

public class HypotenuseCalculations {

    public double hypotenuse(double firstSide, double secondSide) {
        return Math.sqrt(Math.pow(firstSide, 2) + Math.pow(secondSide, 2));
    }
}
